<UserControl x:Class="ExpressCleaner.Views.CleaningView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Очистка системы" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="1" Text="Выберите категории для очистки:" FontSize="16" Margin="0,0,0,10"/>
        
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Padding="10">
            <ScrollViewer>
                <StackPanel x:Name="CategoriesPanel"/>
            </ScrollViewer>
        </Border>
        
        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <TextBlock x:Name="TotalSizeText" Text="Будет освобождено: 0 MB" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BtnClean" Content="Начать очистку" Click="Clean_Click" Padding="20,10" Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="14"/>
                <TextBlock x:Name="StatusText" Text="" VerticalAlignment="Center" Margin="20,0,0,0" FontSize="14"/>
            </StackPanel>
        </StackPanel>
        
        <ProgressBar x:Name="ProgressBar" Grid.Row="4" Height="25" Margin="0,10,0,0" Visibility="Collapsed"/>
    </Grid>
</UserControl>
