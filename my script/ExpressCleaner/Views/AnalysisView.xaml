<UserControl x:Class="ExpressCleaner.Views.AnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Анализ системы" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <Button x:Name="BtnAnalyze" Content="Начать анализ" Click="Analyze_Click" Padding="20,10" Background="#2196F3" Foreground="White" BorderThickness="0" Cursor="Hand" FontSize="14"/>
            <TextBlock x:Name="StatusText" Text="" VerticalAlignment="Center" Margin="20,0,0,0" FontSize="14"/>
        </StackPanel>
        
        <Border Grid.Row="2" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
            <ScrollViewer>
                <StackPanel x:Name="CategoriesPanel" Margin="10"/>
            </ScrollViewer>
        </Border>
        
        <StackPanel Grid.Row="3" Margin="0,20,0,0">
            <ProgressBar x:Name="ProgressBar" Height="25" Visibility="Collapsed"/>
            <TextBlock x:Name="ResultText" Text="" Margin="0,10,0,0" FontSize="16" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</UserControl>
